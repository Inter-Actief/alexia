language: python
python:
  - 3.4
  - 3.5
  - 3.6
# command to install dependencies
install:
  - pip install -r config/requirements.txt
# command to run tests
script:
  - python manage.py test --settings alexia.conf.settings.test
